Make a "settings" to change key configuration. (HTML5 and/or cookies? Per session only?)
Add shortcut(s?) for changing reading type (One page/All pages).
Change bookmark function so that it works while reading the manga.
